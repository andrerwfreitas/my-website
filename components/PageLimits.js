const PageLimits = ({children}) => (
  <>
    <div className="w-screen flex justify-center">
      <div className="w-full max-w-2xl p-5">{children}</div>
    </div>
  </>
);
export default PageLimits;
